const express = require("express");
const router = express.Router();
// const { getFunctionCode } = require("../controllers/code.controller");

// This handles GET /api/code?function=xyz
//router.get("/", getFunctionCode);

module.exports = router;